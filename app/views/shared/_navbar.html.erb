<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand" do %>
      <%= image_tag('WhiskyCodeLogo2.png', style: 'width: 50px; height: auto;') %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'Distilleries', distilleries_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
        <%# I use the link-redirect controller here to force the window.href to load.  It seems
        that for some reason the normal redirect is not reloading the whole page.  I suspect
        it has something to do with turbo. %>
           <%= link_to 'Distillery Heatmap', map_display_path(state: 'PA'), class: 'nav-link', data: { controller: 'link-redirect', action: 'link-redirect#click' } %>
        </li>
        <li class="nav-item">
            <%= link_to 'Products', products_path, class: 'nav-link' %>
        </li>
      </ul>
    </div>
  </div>
</nav>